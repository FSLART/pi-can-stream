# Configuration for rpi-can-stream.service.
PORT=5000
LISTEN_HOST=0.0.0.0

# Set the actual bitrate for each physical bus to configure it at boot.
# Leave empty if another boot service already configures the interfaces.
# Interfaces that are already UP keep their existing configuration.
CAN0_BITRATE=
CAN1_BITRATE=
